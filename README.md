# Manga Downloader → PDF → Google Drive

## Endpoints
/start   → begin background build
/status  → check if still running
/ping    → keep-alive

Files are uploaded to Drive into folder ID in env var: GDRIVE_FOLDER_ID
